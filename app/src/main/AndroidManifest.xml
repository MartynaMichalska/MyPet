<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.petcare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetCare">
        <activity android:name=".addingPicture"></activity>
        <activity android:name=".petInfo02" />
        <activity android:name=".petInfo01" />
        <activity android:name=".pickGender" />
        <activity android:name=".registration" />
        <activity android:name=".Choose_Race" />
        <activity android:name=".Add_Activity" />
        <activity android:name=".Your_account2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>